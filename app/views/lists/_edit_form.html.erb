<%= form_tag user_page_list_path(current_user.id,params[:page_id],@list.id), method: 'put', :class => "form-inline", id: "list_update_#{@list.id}", remote: true do %>
  <%= text_field_tag "list[name]", @list.name %>
  <%= submit_tag 'update list name' %>
<% end %>