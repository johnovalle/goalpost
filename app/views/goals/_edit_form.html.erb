<%= form_tag user_page_list_goal_path(current_user.id,params[:page_id],params[:list_id],@goal.id), method: 'put', :class => "form-inline", id: "goal_update_#{@goal.id}", remote: true do %>
    <%= text_field_tag "goal[name]", @goal.name %>
    <%= submit_tag 'update goal' %>
  <% end %>