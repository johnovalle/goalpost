<%= form_tag user_page_path(current_user.id,page.id), method: 'put', :class => "form-inline", id: "page_update_#{page.id}", remote: true do %>
  <%= text_field_tag "page[name]", page.name %>
  <%= submit_tag 'update page name' %>
<% end %>